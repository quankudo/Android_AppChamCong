<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="@android:color/white"/> <!-- Màu trắng khi được chọn -->
            <stroke android:width="2dp" android:color="@android:color/white"/> <!-- Border trắng -->
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/> <!-- Màu trong suốt khi không được chọn -->
            <stroke android:width="2dp" android:color="@android:color/white"/> <!-- Border trắng -->
        </shape>
    </item>
</selector>